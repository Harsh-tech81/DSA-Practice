#include<iostream>
#include<string>
using namespace std;
int main(){
// string name="Harsh Kumar is a \"Excellent\" boy";    // use of escape character ('\')
// // getline(cin,name);  // new way to print the whole sentence 
// cout<<name;
// cout<<endl;
// char str[100];
// // for(int i=0;str[i]!='\0';i++)
// // cout<<str[i];
// fgets(str,100,stdin);
// puts(str);
// cout<<name.size();  // calculate the size of the string using already made built-in function 
// string sername=" kumar"; 
// cout<<name+sername;  // concatenation of the string using plus symbol in place of strcat() i.e.in-built function for concatenates the two strings 
// name.push_back('p'); // same as a  vector in C++
// cout<<name;
// cout<<endl;
// name.pop_back(); // same as a  vector in C++
// cout<<name;
// cout<<endl;

// string str="\\";  // by using the concept of escape sequence we are able to print one backslash 
// cout<<str<<endl;

// To append the 2 strings
//  string s1="Harsh";
// // string s2="Kumar";
// // cout<<s1.append(s2);   // use of append in place of add the strings 
// s1+='p'; // one new way to add a new character in a string 
// cout<<s1;

// string str;  // printing Null character by using escape character 
// getline(cin,str);
// string ne = str;
// cout<<str<<endl;
// int i=0;
// int j=str.size()-1;
// while(i<j){
//     swap(str[i],str[j]);
//     i++;
//     j--;
// }
// if(ne==str) cout<<" Given string is a palindrome "<<endl;
// else cout<<" Given string is not a palindrome "<<endl;
// cout<<str<<endl;
// int count=0;
// for(int i=0;str[i]!='\0';i++) count++;
// cout<<count;

















return 0;
}