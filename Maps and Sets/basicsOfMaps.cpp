#include <iostream>
#include <unordered_map>
using namespace std;
int main()
{
    // unordered_map<char,char>mp;

    // pair<string, int> p1;  // we have to insert pair in the map
    // p1.first = "Harsh";
    // p1.second = 56;
    // // pair<string, int> p2;
    // // p2.first = "Laksh";
    // // p2.second = 23;
    // // pair<string, int> p3;
    // // p3.first = "Ansh";
    // // p3.second = 12;

    // m.insert(p1);
    // m.insert(p2);
    // m.insert(p3);

    // Another way of inserting into map
    // m["Harsh"] = 56; // very easy way
    // m["Mohnish"] = 96;
    // m["Zaid"] = 16;
    // for(pair<string,int> p : m){
    //     cout<<p.first<<"  "<<p.second<<endl;
    // }
    // cout << m["Harsh"];
    // for (auto p : m) // In foreach loop you can use auto in place of the pair
    // {
    //     cout << p.first << "  " << p.second << endl;
    // }
    // cout << endl;
    // cout << m.size() << endl;
    // m.erase("Harsh");
    // m.erase("Mohnish");
    // m.erase("Zaid");

    // for (auto p : m) // In foreach loop you can use auto in place of the pair
    // {
    //     cout << p.first << "  " << p.second << endl;
    // }

    // cout << m.size() << endl;

// mp['C']=100;
// mp['L']=23;
// for(auto p : mp){
//     pair<int,int>p1=p;
//     cout<<p1.second<<endl;
// }

// mp['A']='B';
// mp['C']='D';
// char ch='S';
// if(mp.find(ch)!=mp.end()){
//     cout<<"find"<<endl;
// }
// else cout<<"No";

cout<<"Hello";


    return 0;
}